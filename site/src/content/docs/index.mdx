---
title: Upfront
description: Force thinking before code. AI-assisted development that keeps humans strong.
template: splash
hero:
  tagline: AI makes writing code effortless. That's the problem.
  actions:
    - text: Why Upfront
      link: /upfront/why/
      icon: right-arrow
    - text: Get started
      link: /upfront/install/
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<CardGrid>
	<Card title="Think first, code second" icon="pencil">
		Every command challenges you before generating anything. The AI asks open questions, waits for your answer, then fills the gaps you missed.
	</Card>
	<Card title="Human-writes mode" icon="seti:lock">
		AI writes the tests. You write the critical code. Tagged in git so you know which code your team actually understands.
	</Card>
	<Card title="Audit trail" icon="list-format">
		Thinking records capture what was decided, why, what was rejected, and what was skipped. 30-second triage for reviewers and managers.
	</Card>
	<Card title="Zero dependencies" icon="puzzle">
		Markdown files in `.claude/commands/`. Copy them into any project. No build step, no SaaS, no API keys.
	</Card>
</CardGrid>

## The flow

```
/ideate → /feature → /plan → /build → /ship → /retro
```

Every step forces thinking. Every transition produces an artifact. The spec is the audit trail of the thinking, not just the conclusions.
